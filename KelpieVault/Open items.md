---
note: The Kelpie checklist — open work in priority order, plus the reviewer nits not taken.
---

# Open items

## Priority order

- [x] **0. Push the repo.** Done 2026-09-11: public `https://github.com/Getterbetter/Kelpie`, remote `origin`, default branch `kelpie`, tag `kelpie-pre-rebase-20260911` pushed too. (His `gh` token needed the `workflow` scope first.)

- [ ] **1. Confirm round 3 on the iPad**: ~~Escape and Cmd+.~~ (confirmed working, `5afab42`) · Option+Backspace deleting one word and only one word · Option+Left/Right jumping words · plain Backspace and Return unchanged · the host capsule top-right, and Hosts / Switch Host from it. If Option+Backspace deletes a word *plus* a character, the UIKit echo arrived after the press ended — see the note in `scheduleHardwareKeyClaimReset`.
- [ ] **1f. Round 5 on the device**: drop from Files · Cmd+←/→/↑/↓ in a shell · tap a file path in Claude's output (Quick Look + share) · Open File on Host… · bell haptic (`printf '\a'`) · font stepping down as the window narrows · desktop notification once the mini config line is in.
- [ ] **1g. Round 6 on the device** (build 6b installed; 6a's screen-sized hold box and sidebar-spanning selection are fixed): double-tap a word → handles → drag a handle (the first build lost the selection on handle touch; fixed) → Copy / Cmd+C · long-press the sidebar edge: a translucent ring appears under the finger (the haptic is silent on iPad), then drag · long-press without moving → herdr's menu on lift. Specs, reviews and the touch map in `Archive/round6/`.
- [ ] **1a. Try media into herdr on the device** (round 4, installed): copy a photo in Photos → Cmd+V in a Claude Code pane inside herdr (expect an upload capsule, then a `/tmp/heeler.…/stage-…/photo.jpg` path typed into the pane) · drag a file from Files in Split View onto the terminal · Attach Photo / Attach File in the Kelpie menu · a paste of plain text still pastes text. Then ask Claude what is in the image.
- [x] **1d. Clipboard out** — works (2026-09-11).
- [x] **1e. Window size** — built in round 5; "split view works well".
- [ ] **1b. See the Welcome screen on the device.** Setup Guide in the Kelpie menu shows it; the no-Host root needs the Host removed first. Try Paste Pairing Code with a fresh code from the mini, and the typed field.
- [ ] **1c. Put the fixed pairing plugin on the mini** — *Anthony will not test the QR himself; low priority.* The installed plugin is upstream's (`github:ZingerLittleBee/Heeler/plugin@main`); the QR clamp fix lives in this repo's `plugin/`. Either link the local checkout into herdr or upstream the fix.
- [ ] **2. On-device verification of round 2.** Nothing in round 2 has been exercised on the iPad beyond launching it. The list: trackpad two-finger scroll inside a pane · trackpad right-click opening herdr's menu · one-finger long-press as a right click · tapping a URL (especially one ending a line) · the keyboard pad appearing when the Magic Keyboard is detached · automatic keyboard mode switching live · the floating menu and the Agents cover · notification deep links landing on the right agent. Detail in [[Testing status]].
- [x] **3. Return-to-submit in the console's Keyboard mode.** Confirmed working on the device 2026-09-12 (on-screen keyboard Return in a shell pane). Original note:
- [x] **3-old. Return-to-submit in the console's Keyboard mode.** Partial fix only: Direct Input now claims first responder while a hardware keyboard is attached, which was a real gap. If Return still fails, the remaining suspect is UIKit loaning Return to the IME under the `.naturalLanguage` text-input traits the agent terminal uses — shell terminals use `.terminal` and are reported working. Needs a device to confirm.
- [x] **4. Deploy our own push relay.** Done 2026-09-11: `kelpie-apns.getter-tilbury-0m.workers.dev`, keyed. Original note kept:
- [x] **4-old. Deploy our own push relay.** Cloudflare Worker from `relay/` with Anthony's own APNs `.p8`, `APNS_TEAM_ID = 8JQWBQKEXX`, `APNS_TOPIC = TME.Kelpie`; point the app (or the in-app Custom Push Relay setting) and the plugin default at it. Cost ≈ zero. **Mandatory for any App Store build**; a personal build could instead have the plugin on the mini call APNs directly. Outward-facing — needs Anthony's explicit yes. Background in [[Heeler upstream]].
- [x] **4b. App Store submission.** Submitted 2026-09-12 02:20 UTC: 1.0 + three tips Waiting for Review (round 9, [[App Store plan]]). Left: delete the Hetzner review host after approval. TestFlight public beta submitted 2026-09-12 too (link in [[App Store plan]]); the host must outlive both reviews.
- [x] **5. TestFlight upload.** Build 1 of 1.0 uploaded 2026-09-12 (VALID). Original note:
- [x] **5-old. TestFlight upload.** Needs his yes. Now the upload step of [[App Store plan]], not a testing round. `scripts/ExportOptions.plist` already carries team `8JQWBQKEXX`; `make bump && make testflight` is the upstream path.
- [x] **6. Rebase on Heeler upstream.** Done 2026-09-11 (round 7): 36 commits replayed onto upstream `375267c` (herdr 0.9.0 wire, muse agent kind, PR #307 paste key cap). Two `CHANGELOG.md` conflicts, nothing else; Release build clean. Pre-rebase history is kept as tag `kelpie-pre-rebase-20260911`. Still a recurring item — see [[Heeler upstream#Rebase strategy]] and `Archive/round7/rebase-summary.md`.
- [x] **9. Key chips above the keyboard.** Round 9: chip row, then reworked into item 12 on his feedback (two rows, round buttons).

- [ ] **10. iPhone build.** Built 2026-09-12 (`4beeecd`, [[iPhone assessment]]): universal, 12 pt phone default, herdr submenu, installed on his iPhone 16 Pro Max. Left: device check on the phone, iPhone screenshots and a 1.1 listing after 1.0 clears.
- [ ] **11. iCloud pairing sync.** Built 2026-09-12 (`4beeecd`, ADR 0018): device key and per-Host records in iCloud Keychain, Settings toggle. Left: the two-device check (pair on the iPad, open the iPhone, the Host should be there and connect; then a notification to each device).
- [ ] **12. Key bar on the device.** One keyboard-styled row replaced the chip bar + paste/newline row (`4beeecd`); round 15 restyled it as one pill (`88cd333`) and Anthony's verdict is in ("looks good"). Left: sticky ctrl then b reaching herdr (now shown as a tinted caption), Paste with a photo.

- [ ] **13. Pairing sync carries edits.** Built 2026-09-12 (round 12): address/port/user adopt from a strictly newer record, local edits republish, reconcile on every Hosts change. Left: device install and the two-device Tailscale switch. Original: Anthony 2026-09-12: his Host address must change from the LAN IP to the Tailscale IP (`100.65.54.52`, user `anthonytopalides`, port 22; the mini advertises no subnet route, so cellular + Tailscale cannot reach `192.168.x`). Today's sync adopts unknown Hosts only; a newer synced record should update an existing Host's address/port/user too (last writer wins by `updatedAt`, never over a local edit that is newer). Small change in `PairingSync.reconcile`.

- [ ] **7. Consider a pull request upstream** for the iPad input work. Anthony's call; deliberately deferred while the fork is private.
- [x] **16. Force-push `kelpie` after the round-11b re-parenting** *(Closed 2026-09-15, round 16: `origin/kelpie` had already been reset, so the pending rounds 14 to 15 pushed as a plain fast-forward; the round-16 rebase was then force-pushed on Anthony's yes.)* (`git push --force-with-lease origin kelpie`, Anthony's yes). Round 11 was written into the vault and the stale hashes repaired in round 12; pre-round-7 hashes still resolve only through `kelpie-pre-rebase-20260911`.
- [x] **17. Load the Reddit watch** *(Closed 2026-09-15: found already bootstrapped, 57 hourly runs since 2026-09-12, and `t1_p9bp6zo` marked answered on 2026-09-12 21:51 UTC; the note said otherwise and is corrected.)* (`launchctl bootstrap gui/$(id -u) scripts/launchd/com.kelpie.redditwatch.plist`, Anthony's yes) and answer the first comment (u/Training_Mail_973, r/SideProject, photo resolution over SSH), then `scripts/redditwatch.py --answered t1_p9bp6zo`. See [[Reddit watch]].
- [ ] **19. No notification while Kelpie was foregrounded.** Anthony 2026-09-12: no notification arrived when the session asked for input while the iPad was unlocked with Kelpie open. What the code does: with the app in the foreground, `willPresent` returns `[]` (no system banner) and the only surface is the in-app banner (`AgentNotificationBannerStore`, drawn at the top of `HerdrClientRootView`), which announces a Blocked/Done transition seen in the Console's live Agent list after a 3 s hold, never repeats an unchanged status, stays silent for the presented Agent (`notificationRouter.path.last`, nil on the root screen), and fails closed when the Host's confirmed notify flags are unknown. **Found on the mini the same night**: `~/.config/herdr/plugins/config/heeler/notifications.json` has one device entry, written 2026-09-11 19:33, `env: sandbox`; the iPad has since moved to TestFlight build 2 (production APNs) and the iPhone adopted the Host by pairing sync, and the file never changed — the app re-registers only from the two Settings toggles (`NotificationPreferencesStore.setNotificationsEnabled`/`setDoneEnabled`), never on launch or on a token/environment change. So every background push to the iPad since build 2 went to the wrong APNs environment and the iPhone has no entry. Fix in flight (round 12b): re-register on launch when the (token, environment) pair changed. Beware: an Xcode-signed install flips the iPad back to sandbox and masks the bug. For the foreground case the remaining candidates are: a finished turn is a `done` transition and the Host's Done trigger is off or unconfirmed; the Console's Agent list is not refreshing while the herdr client owns the screen; the status never held 3 s. Recipe: foreground Kelpie on the root screen, make an agent finish a turn, watch for the top banner; then check the Host's notification triggers in Settings; then pull the log with the key trace running.
- [ ] **20. Round 12c device checklist.** 2026-09-13: (a) done — 1776 tests on the iPad, the seven real failures fixed in `f8201cc` (one product gap: a defeated tombstone blocked republishing a re-paired Host); Release build installed on both devices with the key trace. (f) needs TestFlight build 3 (an Xcode-signed build registers `sandbox` by design). Remaining, Anthony's hands: (b) to (e), (g), (h). Original: (a) full `HeelerTests` on the iPad; (b) double-space in a Claude pane gives `word. `; (c) one-finger scroll drops the keyboard on the iPhone; (d) switch the Host to the Tailscale IP on one device, the other adopts it and connects with a pin (no prompt) or asks (prompt), never a silent reject; (e) a finished turn banners on the root screen; (f) a background push reaches the TestFlight build (its entry on the mini must show `production` in Notification settings); (g) tap on a path reaches herdr, "Open" is in the selection menu; (h) key bar at large text sizes. Then delete the pre-12c build's stale sandbox entry if Settings shows a mismatch.
- [x] **21. Rebase onto Heeler upstream (issue #3).** *(Done 2026-09-15, round 16: 105 commits replayed onto v0.1.8 `b384847`, 14 files conflicted, three reviews, see [[Decisions]] and `Archive/round16/`; device checks are item 33.)* The dependency watch flagged 46 upstream commits and a `project.yml` conflict (PR #298, libghostty-spm bump) on 2026-09-13; the analysis lane's comment on the issue lists the ten files both sides touched. Same recipe as round 7 (Open item 5); needs a device build afterwards.
- [ ] **22. Root screen spins at "Connecting" off Wi-Fi.** *Round 14b: fixed in `173b356` (a bounded end that abandons the dead transport; the trace named `SessionDriver.acquireOperation()` behind the channel close as the wait), reviewed, installed on both devices with the trace on — Anthony retries off Wi-Fi; if it still stalls, pull the trace again. Diagnosis: `Archive/round14/tailscale-hang-diagnosis.md`.* *From the iPhone trace: at the Wi-Fi→cellular path change the primary Host's session marked its transport suspect and awaited `stream.end()` on the dead socket; a sibling Host in the same state ended in 1.6 s and reconnected in 3 s, the primary never logged another line. The hang is the graceful channel close on a silently dead path; fix is a bounded, socket-closing end (in progress). Not a second-client problem: the iPad was killed for the retry.* *Round 14: `ConnectionTrace` built (launch with `-kelpie.connection-trace YES` or the key-trace flag, pull `Documents/connection-trace.log` with `devicectl device copy from`); the three round-13 candidates checked against the code in `Archive/round14/tailscale-candidates.md` — the likeliest cause is a long-lived channel dying on the Tailscale path, which the trace will name. Next: Anthony runs the traced build off Wi-Fi, we pull the log.* Anthony 2026-09-13, Release build of 409eb1a over Tailscale: the Host-settings connection test succeeds, the root client spins until Wi-Fi returns. Diagnosed read-only (`Archive/round13/tailscale-diagnosis.md`): the attach parks in `EventsSession.awaitTerminalTransport` with no deadline and waiters resume only on success or a non-retryable failure, so a retryable loop (most likely `timedOut` on a socket inherited as reusable across a path change made while the session was suspended) shows as a bare spinner; the Console cover would show the reason. `cab2da6` (reviewed, installed) makes the root screen present the session's reconnecting state, reason and a Reconnect button; parked waiters get the acquisition deadline and fail on retryable errors too; a path change while suspended marks the transport suspect. **Anthony's retry on `cab2da6`: "didnt work, hangs on reconnecting"** — the state now surfaces, so the session itself never recovers over Tailscale while Host settings' connection test does. Next: read the Console cover's Host row (the failure reason) off Wi-Fi, and compare what Preflight does that the events session does not (`Archive/round13/tailscale-diagnosis.md` lists the candidates: a socket inherited as reusable, a host-key confirmation with no presenter on the root screen, a deadline Preflight has and the session lacks). A device log pull (`-kelpie.key-trace YES` does not cover connections; `log collect --device` or an in-app trace of `EventsSession` failures may be needed) is the way to get the reason without his hands.
- [x] **23. Artifact links under Claude Code's input bar do not open.** Anthony 2026-09-13: Claude pins artifact links below the text bar; a Mac terminal opens them in the browser, Kelpie should too. Suspect: they are OSC 8 hyperlinks with display text, invisible to `TerminalLinkDetector`'s URL scan of viewport text. Found 2026-09-13: Claude Code on the mini is 2.1.270 at `~/.local/bin/claude` and its bundle carries the OSC 8 constants, so hyperlinks with title text are real; the vendored libghostty has no link-at-point query but fires `GHOSTTY_ACTION_MOUSE_OVER_LINK` (with the URL, OSC 8 included) when `ghostty_surface_mouse_pos` lands on one, and Kelpie never calls `sendMousePos` (it hand-encodes SGR reports for the PTY in `TerminalMouseReporting`), so the callback cannot fire today. Built on branch `kelpie-osc8` (`7e1b410`, 61 tests green on the iPad): text scan first, then the surface's own link hit test. **Blocked**: the vendored package keeps `sendMousePos` internal, the only reachable route moves the core's mouse with no modifier, and libghostty reports a hovered link only with the link modifier (measured on the iPad: nothing for an OSC 8 link or a bare URL at mods 0). Anthony allowed the one edit; built in `8834b2a` (public `sendMousePos(x:y:modifiers:)` wrapper, `KELPIE-PATCHES.md`, 61 tests on the iPad), Release installed on both devices. Left: Anthony taps a pinned artifact link on the device; no upstream PR: `Lakr233/libghostty-spm` `main` already has the same public wrapper since `eb4107b` (2026-09-02); see Open item 25.
- [x] **24. Keyboard dismissal on the iPhone: tap, not scroll.** **Confirmed by Anthony 2026-09-13: "tap to dismiss working".** *Round 14: built (`TerminalTapKeyboardDismiss`, deferred 350 ms past the double-tap window, scroll path removed), reviewed, Release installed on both devices; Anthony confirms on the iPhone: a tap on Claude's output drops the keyboard, a tap on the input row brings it back, scrolling leaves it alone, a double tap still selects.* Anthony 2026-09-13: scroll-to-dismiss (round 12b) "is nice but can make it difficult when trying to scroll through actions and replying to each one - perhaps a tap on the terminal text is what hides the keyboard instead of scroll". Change the dismiss trigger to a tap on the terminal text (a tap that resolves to no link or path), keep scrolling from touching the keyboard; check the tap does not fight the link tap, touch selection or the hold-then-drag.
- [x] **25. Re-vendor GhosttyTerminal past `eb4107b`.** *(Done 2026-09-15, round 16, `b063383`: pinned `7e45d27`, binary `upstream.82938b633ba6`, no patch left; three Kelpie-side edits forced by new non-open conformances.)* *Round 14 prep in `Archive/round14/ghostty-upstream-diff.md`: safe, no override breaks, new binary checksum; own session.* Upstream libghostty-spm `main` (head `7e45d27` on 2026-09-13) carries the public `sendMousePos(x:y:modifiers:)` Kelpie patched in, plus "first-class pointer input" for UIKit that may overlap the round-1 pointer work in `HeelerTerminalView` (ADR 0016). Bump the vendored copy (`project.yml` names the pinned commit; `scripts/fetch-ghostty-artifact.sh` needs the matching xcframework checksum), drop `KELPIE-PATCHES.md`, review the pointer overlap, device build. The depwatch already reports the newer release.
- [x] **26. Live Activity.** **Confirmed by Anthony 2026-09-13: "that's working"; his follow-up is Open item 28.** *Round 14: mapped (`Archive/round14/live-activity-map.md`), no code gap; the per-Host toggle defaults off. Anthony: turn on Live Activity in the Host's settings on iPhone and iPad, start an agent in herdr, check the lock screen / Dynamic Island and that a tap opens the Console.* Anthony 2026-09-13: "live activity". Heeler's Live Activity (agent status on the lock screen / Dynamic Island) is created and driven in `ContentView` above the cover (CLAUDE.md, ADR 0017); confirm it starts from the root screen, what it shows for an agent in herdr's TUI, and whether it works on the iPhone. Decide the content: agent name, Blocked/Done, tap-through to the pane.
- [ ] **27. Keep the session alive longer in the background.** *Round 14: surveyed (`Archive/round14/background-lifecycle-map.md`); decided no change — 20 s of the ~30 s assertion is already used, no honest background mode. Closes when 22 does, since the fast return is the same fix.* Anthony 2026-09-13: "keeping the session alive for longer when backgrounded". The SSH session drops shortly after Kelpie leaves the foreground. Survey what iOS honestly allows (a background task assertion buys ~30 s; `NWConnection`/socket behaviour on suspend; no audio/VoIP tricks), keep the connection through short switches, and make the return fast and silent when a longer life is impossible (round 12c's path observer and the round-13 reconnect surfacing are the base). Relates to 26: a Live Activity updated by push needs no live socket.
- [ ] **28. Notifications and the Live Activity open herdr's own screen.** *Round 14b: built in `173b356`, reviewed (must-fix taken), installed; Anthony confirms a notification tap and a Live Activity tap land on herdr's screen, and that Agents from the menu still opens the Console.* Anthony 2026-09-13: "id prefer if notifications & the live activity just opened back into the main view of herdr which is the direct UI rather than the absracted version that heeler came with". The deep link lands on the root screen (switching the primary Host if the link names another), the Console cover is dismissed, never presented; the pane id is kept in the link but unused (herdr's client cannot be told a pane from outside). Open item 26 confirmed working the same day.
- [x] **29. Keyboard toolbar: Shift+Tab and a collapse-keyboard button.** *Done in round 15 (`88cd333`), confirmed on the iPad.* Anthony 2026-09-15: "The row of buttons above the on-screen keyboard should also have a shift+tab button to alternate Claude models. Should also have a button the collapse the keyboard". Shift+Tab is what Claude Code binds to cycling permission/plan modes and model shortcuts; send the CSI `\x1b[Z` sequence. Collapse = resign first responder, as Notion's trailing keyboard-down button does.
- [ ] **30. On-screen typing through a real text field.** *Next: its own round, design first (see `resume.md`).* Anthony 2026-09-15: "Typing with the on-screen keyboard might need a text box that replicates into kelpie so auto correct and other QOL improvements pass through". Today keys go straight to the PTY, so autocorrect, predictive text, dictation and hold-to-accent never apply. Option: a composing text field above the keyboard that sends on submit (or mirrors per keystroke), with the raw path kept for hardware keyboards and TUI control keys.
- [ ] **32. Hardware Shift+Tab on the iPad is not recognised.** Anthony 2026-09-15: "shift+tab with an attached keyboard on ipad doesnt seem to be recognised." The on-screen `⇧tab` key (round 15) sends CSI Z and works; the Magic Keyboard press goes through `pressesBegan`, where either Ghostty's encoder (which now owns hardware keys after the v0.1.8 rebase and "tells Ctrl-I apart from Tab") or iPadOS's focus system (Shift+Tab is the system focus-backward chord) takes it first. Check with the key trace (`-kelpie.key-trace YES`) whether the press reaches `pressesBegan` at all; if it does not, claim it as a priority `UIKeyCommand` like Escape and Cmd+. (Decisions 2026-09-1x, round 2); if it does, encode CSI Z in `interceptHardwareKey`. Test after the rebase build, since upstream changed this path.
- [ ] **33. Device checks on the rebased build (round 16).** Nothing on the v0.1.8 rebase has been exercised beyond install and launch. With the Magic Keyboard: Escape, Cmd+., Cmd+←/→/↑/↓ (Home/End/PageUp/PageDown), Option+Backspace, Option+←/→, plain Backspace and Return once each (review 2 traced every path, `Archive/round16/review-2.md`). Console in landscape with an Agent open fills the window (the re-added policy). A notification tap and a Live Activity tap land on herdr's screen (28). The key bar's Shift+Tab and a sticky ctrl chord (12). A photo drop and Cmd+V of a photo (the drop delegate moved). An OSC 8 link tap under Claude's input bar (23, the patch is now upstream's). Two-finger long-press selection and a trackpad right-click (the gesture delegate moved). Then the older gates: 22, 20 (e)(f)(g).
- [x] **31. Style the keyboard toolbar like Notion's.** *Done in round 15 (`88cd333`), confirmed on the iPad; the shape is in [[Decisions]].* Anthony 2026-09-15: "The buttons above the keyboard may need better styling - Notion does a good job (see screenshot) of this but open to input." Screenshot saved at `KelpieVault/Design/notion-keyboard-toolbar.png`: one rounded pill floating above the keyboard, evenly spaced monochrome glyphs, disabled actions dimmed, a divider before the trailing keyboard-dismiss button. Do with 29, since the row changes anyway.
- [x] **18. Pairing-sync tests on a device.** Done 2026-09-13: ran on the iPad in the full-suite run and again after the tombstone fix (`f8201cc`); all green. Original: Seven new tests from round 12 compile but have never run (no device was connected). Run the `PairingSync` suite on the iPad with the next install.
- [x] **8. Run the unit suite.** Answered by CI on the fork since 2026-09-12 (round 10, PR #2): every PR into `kelpie` runs the full suite (1648 tests, real-SSH fixtures included) on a GitHub macos-26 runner with an iPad Air 11-inch (M4) simulator. The real-SSH suites are flaky there (upstream sees the same): re-run a red run once before treating it as a regression. This Mac's simulator is still unusable.

- [ ] **14. The mini runs herdr 0.8.2** (`~/.local/bin/herdr`, read 2026-09-12) while the committed schema snapshot is 0.9.0. Nothing is broken (the app enforces a protocol floor), but the load-bearing facts in `CLAUDE.md` were verified on 0.8.0 to 0.9.0 sources; when the mini is upgraded the [[Dependency watch]] reports it and the 0.9.0 facts should be re-verified live.
- [ ] **15. Morning-brief consumer for the watch.** `~/.memoryos/kelpie-depwatch-briefing.json` is written every run; one function in `~/.memoryos/briefing_build.py` (modelled on `territory_update`) would put new medium/high findings in the brief. Outside this repo.

## Reviewer nits not taken

From the two fresh-context reviews. The "should-fix" findings from both rounds were all addressed (round 1 in `01a7923`, round 2 in `58199a7`); these are the ones left deliberately.

### Round 1 — [[Archive/round1/review|review]]

- [ ] **`.detailOnly` assumes a sidebar toggle that may not be visible.** `ConsoleView` hides the sidebar whenever an agent is open on regular width, on the premise that the standard display-mode button brings it back — but the agent terminal deliberately draws an empty, clear-backed nav bar it renders *under*. Nobody has seen this on an iPad. Not a hard trap: `AgentEdgeBackGesture` dismisses and restores `.automatic`. **Check this during round-2 device verification.**
- [ ] **The collapse trigger is "an agent is selected", not "a terminal is open".** A non-empty router path also covers the removed-worktree and missing-agent `ContentUnavailableView`s, so the sidebar hides behind those too. Harmless; both carry their own way back.
- [ ] **The sidebar can stay revealed when switching agents.** Reveal it by hand inside agent A, tap agent B: the preferred value has not changed, so the new terminal is not full-bleed. Deliberate — the alternative fights the manual toggle — and self-correcting on the next navigation.
- [ ] **The two-finger selection sheet loses its anchor.** Ghostty's own path computes an anchor from `quicklookWord()` so the sheet opens with the touched word selected; Kelpie passes `anchorRange: nil`. Per spec; noted so the difference in feel is not a surprise.

### Round 2 — [[Archive/round2/review|review]]

All eight taken in round 7 (2026-09-11, `98187d3`, reviewed — `Archive/round7/nits-review.md`). Kept here for the record; the font-default one was superseded rather than changed.

- [x] **A full viewport read on every tap, twice.** `linkURL(at:)` does a C call plus a whole-viewport UTF-8 copy on the main thread, and runs twice per direct tap and twice per trackpad click (once in `gestureRecognizerShouldBegin`, once in the handler). A short-lived cache keyed on the surface's damage counter would collapse it to one.
- [x] **Wide characters and tabs shift link columns.** The detector indexes by grapheme while the grid mapper counts cells, so a CJK or wide-emoji glyph earlier in a row puts everything to its right off by one per glyph. Rare in herdr's TUI. At minimum it wants a line in the doc comment, which currently claims wrapping is "the one piece of terminal-specific knowledge".
- [x] **The grid mapper clamps to the grid,** so a tap in the bottom or right padding resolves to an edge cell and can open a URL that is not under the finger.
- [x] **`.environment(hardwareKeyboard)` is dead.** `ContentView` injects it and nothing reads `@Environment(HardwareKeyboardObserver.self)` — every consumer takes it as an explicit `let`. Should be one or the other, not both.
- [x] **The font default keys off idiom, not width.** *(Superseded, not changed: round 5's width ladder in `TerminalZoomSettings` runs on first layout with `initial: true`, so a Slide Over launch settles at 10 pt.)* `UIUserInterfaceIdiom == .pad` rather than a regular-width window, so an iPad in a compact Slide Over still starts at 12 pt. A quiet narrowing of the spec.
- [x] **A `UITouch` is retained strongly** in the link-claim state, where round 1's right-button equivalent is `weak`. UIKit's guidance is not to retain touch objects past the event; a stale claim is inert rather than harmful thanks to the `touches.contains` guard.
- [x] **`.onDisappear { notificationRouter.path = [] }`** sits on a view used both as the cover's content and as the no-host root, so adding the first Host clears the path as a side effect. Harmless today.
- [x] **The menu button raises opacity on hover but not on press.** The spec said "hover/press"; the menu dims the screen anyway.

## Known and accepted

- **Push notifications** now go through Kelpie's own relay; the pipeline was verified with a hand-run hook (APNs answered through the relay). A real delivery to the iPad is still to be observed by Anthony.
- **Unit tests cannot be executed on this Mac.** CI on the fork runs them instead (item 8); do not spend more time on the local simulator.
- **The test target cannot compile for a device destination** — `SidebarConsoleIntegrationTests` depends on simulator-only demo code. Pre-existing upstream.
- **Only the forward wrapped-URL join is implemented:** a tap on the *continuation* row of a wrapped URL returns nil. The first row is the larger target.

Related: [[Kelpie]] · [[Testing status]] · [[Decisions]]
