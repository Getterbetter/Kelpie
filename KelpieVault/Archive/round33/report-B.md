# Round 33 — Builder B report (transport and terminal group)

Branch `round33-B`, worktree `$SP/wt-B`.

## Commits

1. `d0eb616a` never autocorrect on any terminal input surface — **picked** as `b6d8d7d4`. Conflict: `project.pbxproj` only (took Kelpie's side, `xcodegen generate`). The pick touches upstream's Console composer (`AgentComposerUITextView`) and the terminal (`HeelerTerminalView`), not Kelpie's root composer (`TerminalComposerTextView`, which sets `autocorrectionType = .default` in its own init). Added `kelpieComposerFieldKeepsAutocorrectionOn` to the new `TerminalInputTraitsTests` to pin that. Build/test: pending.
2. `c6135ec5` release a replaced surface's keyboard after the graph update — **skipped**. Its fix lives in `TerminalSurfaceRetention.detachCallbacks` (plus a new `HeelerTerminalView.retireLocalInput`) and `TerminalSurfaceRetentionTests`; that class and suite arrived upstream with `b7de27d1` (feat(console): browse and retain workspace terminals), which is after v0.1.8 and not in Kelpie, so the code it fixes does not exist here. Kelpie's own overlap (`HerdrClientView.mountedSurfaces`, a `ForEach` of current + retired) retires a surface by setting `isLocalInputEnabled = false` on it, and `setLocalInputEnabled(false)` resigns first responder synchronously from `updateUIView`. That is intentional ("Disabling input dismisses its keyboard, as dismantling the view used to") and has not crashed on Kelpie's devices, but it is the same shape upstream saw abort in AttributeGraph on iOS 27 (resign during a graph update). Not changed here (out of scope); worth an Open item if iOS 27 shows the SIGABRT on a pane switch.
3. `2b5f4e6c` composer waits out agent_not_ready — **picked** as `b13e5d8a`. Conflict: `CHANGELOG.md` only (upstream's context was its 0.1.9 section); the #368 entry went under Kelpie's `[Unreleased] ### Fixed`. Source and tests merged cleanly.
4. `c48346f1` wait out the silent transport replacement (composer sends) — **picked** as `6f6082b1`. Conflicts: `CHANGELOG.md` (entry under Kelpie's `[Unreleased] ### Fixed`) and two hunks in `EventsSession.swift`, both adjacency only: upstream's new `awaitUsableTransport` sits before Kelpie's `withTerminalTransport` doc comment (Kelpie's single-channel wording kept), and upstream's `TransportWaiter` struct before Kelpie's `acquireTerminal()` (Kelpie has no `target:` parameter; its version kept). The waiter release points landed in Kelpie's code: `failTransportWaiters` beside every `failTerminalTransportWaiters` in the run loop (both retryable and non-retryable branches) and in `windDown`, and the resume in `resumeTerminalTransportWaiters` after Kelpie's bounded, generation-checked terminal waiters. Kelpie's abandon (`endStreamPromptly`, `19277ce8`) is untouched: an abandoned transport reads `!isConnected`, `ensureTransport` nils `currentTransport` and dials, and that gap is exactly the window the new waiters cover.
5. `cd2972c1` wait out the silent replacement before failing a send — **picked** as `8b6ac73f`. Conflict: `CHANGELOG.md` only (upstream's amended sentence applied to the entry from pick 4). `EventsSession.swift` merged cleanly: `withTransport` retries once on `.sshUnreachable`/`.timedOut`, marking the transport suspect and clearing `currentTransport` only while it is still the installed one.
6. `34d3af8a` bound the dead-transport retry — **picked** clean as `9992726c`.
7. `45fcfd7d` wake the run loop for the dead-transport redial — **picked, adapted** as `fced8aad`. Conflict: `ConsoleStoreTests.swift`, where upstream edits focus tests (`focusAgent…`) that Kelpie does not have (no `focusAgent` in Kelpie's sources or `ScriptedTransport`); took Kelpie's side, dropping them. `HostConsoleProjection.swift` (worktree-removal re-authorization) merged cleanly. Adaptation in `EventsSession.withTransport`: upstream wakes the parked run loop with a plain `await liveStream?.end()`; that graceful close on a transport that has just failed at the link level is the Open item 22 hang (close queued behind the driver's deadline-less operation mutex). Kelpie's version calls `endStreamPromptly(stream)` (2 s bound, then `abandon()`), and sets `resubscribeRequested` only when a stream is live, so a stale flag cannot later turn a genuine failure into a silent resubscribe. Noted on the commit. Open question left as upstream has it: the retry path nils `currentTransport` without closing the old transport, so `ensureTransport` dials past it rather than closing it; after an abandon the connection is already invalidated, after a graceful end within 2 s it is dropped by reference only.
8. `c18e8287` poll for the coalesced keyboard height — **picked** as `e9834645`. Conflict: `AgentDirectInputTests.swift`, the same two assertions Kelpie had already loosened to a 250 ms fixed sleep; upstream's poll (`Self.eventually`) replaces both. Only those two hunks changed; Kelpie's hardware-keyboard guards, `presentsSoftwareKeyboard` conditions and `probe()` fallback in the file are kept.

## Build and tests
- Compile: `build-for-testing`, generic iOS: **TEST BUILD SUCCEEDED**.
- Device tests, first attempt: xcodebuild could not reach the iPad ("observing system notifications failed … Ensure that the device is unlocked"). Retrying once.
- Device tests, retry (iPad Pro 11-inch, 09D7738D): **356 tests in 11 suites passed, 0 issues** (`EventsSessionSubscriptionsTests`, `EventsSessionTerminalChannelTests`, `ConsoleStoreTests`, `AgentComposerStoreTests`, `AgentDirectInputTests`, `TerminalAttachTests`, `TerminalComposerMirrorTests`, `TerminalComposerControlTests`, `TerminalInputControllerTests`, `HerdrClientStoreTests`, and the new `TerminalInputTraitsTests` including `kelpieComposerFieldKeepsAutocorrectionOn`). `TerminalSurfaceRetentionTests` does not exist in Kelpie (see pick 2). The first attempt failed to reach the device (likely another builder holding it); the retry reached it with no change.
- Skipped with a reason (Magic Keyboard docked, "needs the software keyboard"): `aDroppedPresentationSettlesAgainstTheWindowsLiveKeyboardLayoutGuide`, `theComposerSettlesItsHandoffAgainstTheLiveKeyboardLayoutGuide`, `toolsKeyboardSurvivesAgentHandoffAndResumesSystemInset`, `toolsPreserveTheEditorAndSendShortcutsWithoutChangingTheDraft`. `composerAndDirectInputTransferVisibleKeyboardWithoutReloading` returns early on a hardware keyboard (Kelpie's guard), so it passes without exercising anything.

## Left, unsure, and what to look at

- Nothing left to pick. `c6135ec5` skipped (code not in Kelpie); Kelpie's analogous synchronous resign in `setLocalInputEnabled(false)` from `updateUIView` for the retired surface is a candidate Open item if iOS 27 aborts on a pane switch.
- Unsure: the orphaned old transport on the `withTransport` retry path (pick 7 note), inherited from upstream; and `resubscribeRequested` is now set only with a live stream, a small departure from upstream.
- CHANGELOG: two #368 entries added under Kelpie's `[Unreleased] ### Fixed`; other builders also edit that section, so expect a textual merge there.
- Orchestrator should look at: `EventsSessionSubscriptionsTests` (upstream's new silent-replacement and redial tests) and `ConsoleStoreTests` on the iPhone too, the four keyboard tests above with the keyboard undocked, and `TerminalInputTraitsTests` on both devices.
