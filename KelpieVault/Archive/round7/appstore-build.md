# App Store prep build (round 7b, 2026-09-11)

Opus builder, spec in the session. Changes: visible "Heeler" text → "Kelpie" in `project.yml` (camera usage, extension display names) and 17 Swift files (Settings, notifications, Live Activity, agent-list editors, composer staging alerts, theme preview); `NOTICE`; `Sources/Heeler/Resources/Notices/Heeler-Apache-2.0.txt` plus an `inventory.json` entry; `PRIVACY.md` rewritten; `Sources/Heeler/Client/KelpieLinks.swift` (repository, privacy policy, support); `INFOPLIST_KEY_NSLocalNetworkUsageDescription`; `relay/wrangler.toml` name/route/topic; `scripts/publish.sh` `PUBLISH_REMOTE`/`PUBLISH_BRANCH`; 1.0.0 (1) on all three targets. Left: Keychain services and access groups, logger subsystem, module/target names, wire literals (`heeler` URL scheme, plugin id, `heeler-enroll`, authorized_keys comment), third-party notices, deliberate upstream references in `WelcomeView` and `NotificationPreferencesStore`. Release device build succeeded.

## Review (Opus, fresh context)

Wire-safe and spec-complete. Must-fix: two test suites still asserted the old strings (`ConsoleDetailPresentationTests:428`, `AgentListFieldsEditorTests:117,331,498`) — fixed. Should-fix taken: `APNS_TEAM_ID` in `wrangler.toml` was upstream's team — set to 8JQWBQKEXX with the key id left as a TODO; `PRIVACY.md` relay sentence read "at ." — reworded; `Makefile` `APP_ID` → `TME.Kelpie`; relay `FALLBACK_ALERT.title` → "Kelpie" in lockstep with its test and the app's renderer test. Not taken: the relay route rename vs. the app's still-upstream endpoint (deliberate — the endpoint changes when the relay is deployed, gate 2 in `App Store plan.md`); `make publish` deriving 0.1.7 from CHANGELOG (documented: pass `VERSION=1.0.0` the first time, or reset the CHANGELOG before publishing).
