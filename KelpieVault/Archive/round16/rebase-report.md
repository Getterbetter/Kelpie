# Rebase kelpie-upstream-20260915 onto upstream/main (b384847, Heeler v0.1.8)

## Stops
1. f87add4 rebrand — project.yml: Kelpie bundle IDs/display name/team kept; version left at Kelpie's 0.1.6/18 so the later Kelpie bump to 1.0/3 applies.
2. c1ba587 — project.pbxproj: took Kelpie's, regenerated at the end.
3. 33abd82 vendor Ghostty — project.yml: kept `path: Packages/GhosttyTerminal`, dropped upstream's remote url/revision pin. Package.resolved: Kelpie's.
4. 08462b1 iPad pointer — TerminalScreenView.swift: kept both sides' new stored properties.
5. 23c4a30 "an open terminal fills the iPad window" — SKIPPED (judgement call): upstream v0.1.8 ships ConsoleSplitPresentation/ConsoleSplitVisibilityState, which implements the same detail-only behaviour more thoroughly. Kelpie's 17-line version would have replaced it.
6. 93aa06e herdr client is the root screen — ConsoleView.swift: upstream toolbar kept, only Kelpie's `onClose` Done item re-inserted. ContentView.swift: adopted upstream's HeelerAppModel store ownership + scene machinery, body swapped to HerdrClientRootView reading from `app.*`, added `hardwareKeyboard`. HeelerApp.swift (edited as part of this resolution): `WindowGroup(for: AgentRoute.self)` -> one plain `WindowGroup`, route held in a `@State` (principle 2). NOTE: ADR 0017 and CLAUDE.md's root-screen paragraph need a sentence about HeelerAppModel owning the stores.
7. 5a31de8 keyboard mode — ConsoleView: upstream sheet structure kept, only `inputMode:` added to SettingsView. ContentView: upstream env modifiers kept + Kelpie's hardwareKeyboard onChange (now app.inputMode).
8. 9513ae6 Escape/Option word keys — CHANGELOG both lists; TerminalScreenView: upstream pressesBegan/Ended kept, Kelpie's interceptHardwareKey inserted on the non-zoom path (judgement call: intercept runs before the armed-modifier path so Kelpie's Escape/Cmd+./Option word keys win) and pressesEnded now runs both the armed-modifier cleanup and forwardablePresses.
9. 817418d Cmd+. as Escape — TerminalScreenView: key-trace logging prepended, upstream's sceneCommand routing kept.
10. 82ddb88 onboarding — CHANGELOG both lists.
11. f7109e5 photos/files drop — TerminalScreenView: kept upstream pressesBegan and added Kelpie's ⌘V paste check ahead of the scene-command routing (judgement call); isPasteShortcut kept alongside upstream's key-mapping switch.
12. 1539f65 Mac-vs-iPad gaps — project.yml: kept Kelpie's four-orientation comment. ContentView: upstream start-up (HeelerAppModel.start) kept; only Kelpie's TerminalDesktopNotificationRelay .task added, the rest of Kelpie's .task list is now inside HeelerAppModel.start().
13. 7452318 touch selection — TerminalScreenView: Kelpie's ⌘C + clearTouchSelection kept ahead of the existing ⌘V/scene-command/zoom path; Kelpie's duplicate zoom block dropped in favour of upstream's.
14. 64fafc5 reviewer nits — ContentView: dropped .environment(hardwareKeyboard) as the commit intends, kept upstream's sceneWindow/agentSceneRouting environment values.
15. 8158e25 rebrand leftovers — ComposerStagingStore: upstream's failure/publish structure kept, wording rebranded to Kelpie.
16. 540b8d6 tip jar — ContentView: app.* store references kept, tipJar argument added.
17. a756361 keyboard chip row — ShellTerminalView: Kelpie's showsModePicker gate kept around upstream's picker (upstream's InputChromeLayout width, not the hardcoded 184). TerminalScreenView: both new properties kept.
18-65. Not logged by the previous builder (it stopped at its turn limit at stop 66); resolutions unrecorded.
66. e157a21 iPad CI simulator — CLAUDE.md: upstream's `make test`/`make test-ipad` sentence kept, destination and SIM default rewritten to iPad Air 11-inch (M4) (judgement call: the Makefile default is the iPad, so upstream's iPhone 17 wording would be false). Makefile: both vars kept, SIM = iPad Air 11-inch (M4), SIM_IPAD = iPad Pro 13-inch (M5). run-ci-ios-tests.sh: upstream's prefix-match selection code and `ci_simulator_name` variable kept, default sourced from `${HEELER_CI_SIM_MODEL:-iPad Air 11-inch (M4)}` (ci.yml references that name). ci.yml: already staged with the fork's artifact fetch + iPad.
71. 4beeecd iPhone build / key bar / iCloud pairing sync — ContentView: HEAD (upstream scene machinery, onContinueUserActivity) kept on all three hunks; the commit's per-window `pairingSyncSettings`/`pairingSync` @State dropped and their construction + wiring moved into HeelerAppModel (judgement call, principle 2: HeelerAppModel is the composition root, and a per-window PairingSync would reconcile once per scene). HeelerAppModel: new `pairingSyncSettings`/`pairingSync` lets, didRemoveHost hook + reconcile in start(), reconcile on .active in scenePhaseDidChange, hostsDidConnect inside the hostStatuses observer, and a new observer on isEnabled. ContentView keeps `.environment(app.pairingSyncSettings)`. TerminalScreenView: HEAD's initialKeyboardMode kept, the commit's "key bar" rewording of the showsKeyBar doc comment taken.
79. a9fcba0 round 12 (pairing sync carries Host edits) — ContentView: HEAD kept whole; the commit's only new behaviour, reconciling pairing sync when `hostStore.hosts` changes, added to HeelerAppModel's existing hosts observer (same reasoning as stop 71).
80. 39ced10 round 12b/12c robustness — CHANGELOG: both lists. AgentNotificationCenterDelegate: the commit's new willPresent method merged cleanly but referenced a `router` property this shape no longer has; rewritten onto the scene directory (`directory.keyScenePresentedAgent`, `directory.open(target)`) while keeping Kelpie's `.unreadableNotification` notice on a nil target (judgement call, principle 3 — directory.open routes through the same AgentNotificationRouter). ContentView: HEAD kept on all four hunks, plus the commit's `.hostKeyConfirmation()`. Everything else the commit added to ContentView was ported to HeelerAppModel: NetworkPathObserver + its run task, `registrationNotes: notificationPreferences` on HostLiveActivityCoordinator and PairingSync, `reregisterChangedDevices()` in the hostStatuses and deviceToken observers, the deviceToken re-run of hostsDidConnect, and `notificationPreferences.hostWasRemoved` on didRemoveHost. TerminalScreenView: both sides kept (initialKeyboardMode + isHardwareKeyboardConnected; key-trace log then the backspace-echo consume).
81. 173b356 Tailscale hang / taps land on herdr's screen — principle 3, Kelpie wins. AgentNotificationRouter: both sides kept (upstream's isKnownAgent plus Kelpie's `landing`/`Landing`/`land(onHostID:)`). ContentView: Kelpie's `notificationRouter.land(onHostID: link.hostID)` for onOpenURL, replacing upstream's sceneDirectory.open. AgentNotificationCenterDelegate: Kelpie's land-on-Host tap handling, routed through the scene directory (the delegate holds no router here). AgentSceneDirectory: new `land(onHostID:)` — key window else most recent, activate it, never open the Console — plus a `pendingLanding` mirroring `pendingOpen` for a killed-state launch (judgement call: the delegate must reach a window's router, and the directory is the only path to one).
82. 88cd333 key bar (Shift+Tab, hide-keyboard, one pill) — TerminalKeyboard.swift: HEAD's upstream file taken whole (AgentQuickKey et al), with Kelpie's `TerminalControlKey` enum (including the commit's new `.shiftTab`) reinstated alongside it, because upstream deleted that enum and TerminalKeyBar.swift — a Kelpie file replayed earlier — is built on it (principle 4's exception). TerminalAttachTests: both sides; the commit's rows test re-closed (the conflict cut it mid-function). TerminalKeysKeyboardTests: only `shiftTabEncodesBackTab` kept from the commit; its two other new tests drive `TerminalControlPadView`, which upstream removed (judgement call — they cannot compile and the pad is gone).

## Completion
- Rebase finished: 105/105 replayed, head 38c9f3d (last replayed commit b063383, re-vendor GhosttyTerminal).
- xcodegen generate: clean. Wire-type check (`--check --schema scripts/herdr-schema.json`): up to date.
- Release build (generic/platform=iOS): 21 errors in round 1, 3 distinct causes; clean in round 2. Debug build-for-testing: clean first try. Logs: build.log, build-tests.log.
- Compile-fixes commit 38c9f3d: TerminalKeyboard.swift (restored the private TerminalEscapeSequences table; added sendControlKey routed through the quick-key encoder), TerminalScreenView.swift (one pressesCancelled instead of two, upstream's cleanup feeding Kelpie's forwardablePresses), Heeler.xcodeproj/project.pbxproj (regenerated).
- Post-rebase facts: 105 commits on upstream/main..HEAD; 80 files / +11754 / -1271 vs kelpie-pre-rebase-20260915 over Sources Tests scripts project.yml; project.yml has PRODUCT_BUNDLE_IDENTIFIER TME.Kelpie (+ .NotificationService), MARKETING_VERSION 1.0, CURRENT_PROJECT_VERSION 3, TARGETED_DEVICE_FAMILY "1,2".
- Flag for review: TARGETED_DEVICE_FAMILY is "1,2" (commit 4beeecd's iPhone build), while the CI simulator script's comment still reads "Kelpie is iPad-only (TARGETED_DEVICE_FAMILY 2)". The iPad default is kept either way; the comment is now stale.
- Not done: no tests run, nothing pushed, the vault not reconciled.
